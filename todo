1. test map exceptions
2. test timing iterators
3. test timing stack
4. test algorithms (`lexicographical_compare` and `equal`)
5. test `type_traits`
6. use stl `iterator_tags` to be compliant with stl `algorithms`
7. why vector iterator `operator==` need to be templated unlike map iterator