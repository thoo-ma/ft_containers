1. test timing iterators
2. test timing stack
3. test algorithms (`lexicographical_compare` and `equal`)
5. test `type_traits`
6. use stl `iterator_tags` to be compliant with stl `algorithms`
7. why vector iterator `operator==` need to be templated unlike map iterator
8. why can't we remove map `value_compare` constructor by default